version: 2
jobs:
  build:
    docker:
      - image: circleci/python:3.7.6
    working_directory: ~/repo

    steps:
      - checkout
      - run:
          name: migrate rerender and automerge
          command: |
            cd ..
            ./repo/scripts/migrate_rerender.sh

workflows:
  version: 2
  hourly:
    jobs:
      - build
    triggers:
      - schedule:
          cron: "0 * * * *"
          filters:
            branches:
              only:
                - master
